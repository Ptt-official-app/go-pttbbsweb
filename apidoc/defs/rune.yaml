Rune:
  type: object
  properties:
    text:
      type: string
      descript: string (in json-unicode) with same color
    color0:
      '$ref': '#/definitions/Color'
    color1:
      '$ref': '#/definitions/Color'
